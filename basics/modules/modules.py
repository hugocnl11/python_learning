#module = a file containing python code. May contain functions, classes, etc.
# used with modular programing, wich is to separate a program into parts

import messages as msg

msg.hello()
msg.bye()

help("modules") # to check all the modules list